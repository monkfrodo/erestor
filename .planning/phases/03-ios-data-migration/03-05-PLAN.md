---
phase: 03-ios-data-migration
plan: 05
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [IOS-01, IOS-02, IOS-03, IOS-04, NOTF-02, MIGR-01, MIGR-02, MIGR-03]
gap_closure: true

must_haves:
  truths:
    - "iOS app builds successfully for iOS Simulator target"
    - "All Swift files compile without errors under the ErestorApp-iOS scheme"
    - "Backend APNs integration code is committed in git"
  artifacts:
    - path: "ErestorApp/ErestorApp.xcodeproj"
      provides: "Generated Xcode project with iOS target"
    - path: "~/claude-sync/produtividade/api/routers/events.py"
      provides: "APNs integration in poll scheduler"
      contains: "_send_apns_for_poll"
  key_links:
    - from: "project.yml"
      to: "ErestorApp.xcodeproj"
      via: "xcodegen generate"
      pattern: "xcodegen"
---

<objective>
Close the two verification gaps from Phase 3: (1) confirm backend APNs commits are persisted in git, (2) run the actual iOS build to verify all Swift code compiles for the iOS Simulator target.

Purpose: Phase 3 verification found that no iOS build was ever executed (iOS SDK was not installed during plan execution). The SDK is now installed (iOS 26.2). This plan runs the build to confirm the entire iOS codebase compiles.
Output: Verified iOS build (zero errors) and confirmed APNs git state.
</objective>

<execution_context>
@/Users/imacke/.claude/get-shit-done/workflows/execute-plan.md
@/Users/imacke/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ios-data-migration/03-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify APNs commit state and run iOS Simulator build</name>
  <files></files>
  <action>
    Step 1 -- Verify Gap 1 (APNs commits) is already resolved:
    Run `cd ~/claude-sync && git log --oneline -5 -- produtividade/api/routers/events.py produtividade/tests/test_apns_integration.py` and confirm commit 4fc7ba2 (or similar) exists with APNs integration changes. Also confirm `git status --short` shows no uncommitted changes to these files. If somehow still uncommitted, stage and commit them with message `fix(03): commit APNs integration changes`.

    Step 2 -- Run iOS build:
    Run from ~/projetos/erestor/ErestorApp/:
    ```
    xcodegen generate && xcodebuild -scheme ErestorApp-iOS -destination 'platform=iOS Simulator,name=iPhone 17 Pro' build 2>&1
    ```
    Note: Use "iPhone 17 Pro" as available simulator (not "iPhone 16" which may not exist with iOS 26.2 SDK).

    Step 3 -- If build fails, diagnose and fix compilation errors:
    - Missing imports: Add required `import` statements
    - Type mismatches: Fix using existing patterns from macOS target
    - Platform guards: Ensure all macOS-only code has `#if os(macOS)` guards
    - Missing files in target: Check project.yml sources excludes

    Step 4 -- Run macOS build to confirm no regressions:
    ```
    xcodebuild -scheme ErestorApp -destination 'platform=macOS' build 2>&1
    ```
    Confirm macOS build still passes after any iOS fixes.
  </action>
  <verify>
    <automated>cd ~/projetos/erestor/ErestorApp && xcodegen generate && xcodebuild -scheme ErestorApp-iOS -destination 'platform=iOS Simulator,name=iPhone 17 Pro' build 2>&1 | tail -5 | grep -q "BUILD SUCCEEDED" && echo "iOS BUILD OK" || echo "iOS BUILD FAILED"</automated>
  </verify>
  <done>
    - `xcodebuild -scheme ErestorApp-iOS` exits with BUILD SUCCEEDED
    - `xcodebuild -scheme ErestorApp` (macOS) also exits with BUILD SUCCEEDED
    - APNs integration commits exist in claude-sync git history
  </done>
</task>

</tasks>

<verification>
- iOS Simulator build completes with zero errors
- macOS build still passes (no regressions from any fixes)
- `git log` in ~/claude-sync shows APNs commits for events.py and test_apns_integration.py
</verification>

<success_criteria>
Both verification gaps from 03-VERIFICATION.md are closed:
1. Backend APNs code is committed in git (was already resolved, confirmed)
2. iOS build succeeds on iOS Simulator with iOS 26.2 SDK
</success_criteria>

<output>
After completion, create `.planning/phases/03-ios-data-migration/03-05-SUMMARY.md`
</output>
