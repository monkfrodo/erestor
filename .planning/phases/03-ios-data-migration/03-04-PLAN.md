---
phase: 03-ios-data-migration
plan: 04
type: execute
wave: 2
depends_on: [03-02]
files_modified:
  - ~/projetos/erestor/ErestorApp/ErestorApp/Views/iOS_PollSheetView.swift
  - ~/projetos/erestor/ErestorApp/ErestorApp/Views/iOS_GateSheetView.swift
  - ~/projetos/erestor/ErestorApp/ErestorApp/iOS/ErestorApp_iOS.swift
  - ~/projetos/erestor/ErestorApp/ErestorApp/Views/iOS_TabRootView.swift
  - ~/claude-sync/produtividade/api/routers/events.py
  - ~/claude-sync/produtividade/tests/test_apns_integration.py
autonomous: true
requirements: [IOS-03, IOS-04, NOTF-02]
user_setup:
  - service: apple-developer
    why: "APNs push notifications require a .p8 key from Apple Developer portal"
    env_vars:
      - name: ~/.erestor_apns_config
        source: "JSON with key_id, team_id, bundle_id from Apple Developer > Keys > APNs"
      - name: ~/.erestor_apns_key.p8
        source: "Download .p8 key file from Apple Developer > Keys"
    dashboard_config:
      - task: "Create APNs Key (if not already done)"
        location: "Apple Developer > Account > Keys > Create Key > Enable Apple Push Notifications service"

must_haves:
  truths:
    - "Energy and block quality polls appear as modal bottom sheets on iOS"
    - "Poll sheets slide up, user taps a response option, sheet slides away"
    - "Gate alerts appear as modal sheets with severity indication and task list"
    - "Push notifications arrive on iOS with inline action buttons for polls"
    - "Energy notification has buttons 1-5 (grouped to fit 4-button limit) plus Remind in 10min"
    - "Block quality notification has buttons perdi/meh/ok/flow"
    - "Gate alert notification has urgent sound"
    - "Responding to a notification action POSTs to /v1/polls/{id}/respond"
    - "Backend poll scheduler sends APNs push when triggering polls/gates"
  artifacts:
    - path: "ErestorApp/ErestorApp/Views/iOS_PollSheetView.swift"
      provides: "Modal bottom sheet for energy and quality polls with tap-to-respond"
      contains: "presentationDetents"
    - path: "ErestorApp/ErestorApp/Views/iOS_GateSheetView.swift"
      provides: "Modal bottom sheet for gate alerts with severity and tasks"
      contains: "presentationDetents"
    - path: "ErestorApp/ErestorApp/iOS/ErestorApp_iOS.swift"
      provides: "Notification categories with action buttons, response handler"
      contains: "UNNotificationCategory"
    - path: "~/claude-sync/produtividade/api/routers/events.py"
      provides: "APNs push sending in poll scheduler"
      contains: "apns.*send_push"
    - path: "~/claude-sync/produtividade/tests/test_apns_integration.py"
      provides: "Tests for APNs integration in poll scheduler"
      contains: "test_apns"
  key_links:
    - from: "iOS_TabRootView.swift"
      to: "iOS_PollSheetView + iOS_GateSheetView"
      via: ".sheet(item:) binding on activePolls/activeGates"
      pattern: "sheet.*activePoll"
    - from: "ErestorApp_iOS.swift"
      to: "/v1/polls/{id}/respond"
      via: "UNNotificationCenter delegate action handler"
      pattern: "polls.*respond"
    - from: "events.py"
      to: "erestor/apns.py"
      via: "APNs.send_push() called after push_event()"
      pattern: "apns.*send_push"
---

<objective>
Build iOS poll/gate modal sheets for inline responses, register notification categories with action buttons for push notifications, wire the notification response handler to POST back to the API, and integrate APNs sending into the backend poll scheduler.

Purpose: IOS-03 requires inline polls on iOS (as modal sheets per locked decision). IOS-04 and NOTF-02 require push notifications with inline actions via APNs. The existing apns.py module handles the sending; this plan wires it into the poll scheduler.

Output: Complete poll/gate flow on iOS: SSE triggers modal sheet when app is open, APNs push when app is backgrounded, notification actions POST response to backend.
</objective>

<execution_context>
@/Users/imacke/.claude/get-shit-done/workflows/execute-plan.md
@/Users/imacke/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ios-data-migration/03-02-SUMMARY.md
@~/projetos/erestor/ErestorApp/ErestorApp/iOS/ErestorApp_iOS.swift
@~/projetos/erestor/ErestorApp/ErestorApp/Views/iOS_TabRootView.swift
@~/projetos/erestor/ErestorApp/ErestorApp/Views/PollCardView.swift
@~/projetos/erestor/ErestorApp/ErestorApp/Views/GateAlertView.swift
@~/projetos/erestor/ErestorApp/ErestorApp/Models/SSEEvent.swift
@~/projetos/erestor/ErestorApp/ErestorApp/Services/ErestorConfig.swift
@~/projetos/erestor/ErestorApp/ErestorApp/ErestorApp.swift
@~/claude-sync/produtividade/api/routers/events.py
@~/claude-sync/produtividade/erestor/apns.py

<interfaces>
From SSEEvent.swift:
```swift
struct PollSSEEvent: Identifiable, Codable {
    var id: String { pollId }
    let pollId: String
    let pollType: String  // "energy" or "block_quality"
    let question: String
    let options: [String]
    let expiresAt: String?
}
struct GateSSEEvent: Identifiable, Codable {
    var id: String { gateId }
    let gateId: String
    let message: String
    let severity: String  // "amber" or "red"
    let tasks: [String]?
}
```

From ErestorApp.swift (macOS notification pattern -- reference for iOS):
```swift
// macOS registers POLL_ENERGY, POLL_QUALITY, GATE_INFORM, POLL_REMINDER categories
// Action identifiers: ENERGY_1..ENERGY_5, QUALITY_perdi/meh/ok/flow, REMIND_10
// Response handler parses action identifier and POSTs to /v1/polls/{pollId}/respond
```

From apns.py:
```python
class APNs:
    def send_push(self, device_token, title, body, category=None, data=None) -> dict
    @property
    def is_configured(self) -> bool
```

From events.py (poll scheduler pattern):
```python
# _poll_scheduler runs as asyncio task alongside SSE
# After creating a poll via push_event("poll", {...}), also send APNs
# Polls: energy (every 2h+ during work hours), block_quality (on event end)
# Gates: gate alert (on block end with P1 tasks open)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: iOS poll/gate modal sheets and notification categories with action handler</name>
  <files>
    ~/projetos/erestor/ErestorApp/ErestorApp/Views/iOS_PollSheetView.swift
    ~/projetos/erestor/ErestorApp/ErestorApp/Views/iOS_GateSheetView.swift
    ~/projetos/erestor/ErestorApp/ErestorApp/iOS/ErestorApp_iOS.swift
    ~/projetos/erestor/ErestorApp/ErestorApp/Views/iOS_TabRootView.swift
  </files>
  <action>
    1. Create iOS_PollSheetView.swift (new file, #if os(iOS)):
       - Takes poll: PollSSEEvent, onResponse: (String) -> Void
       - VStack layout:
         - Question text (DS.font(18, .semibold), DS.text color)
         - If poll.pollType == "energy": 5 buttons in HStack (1-morto, 2-baixa, 3-ok, 4-boa, 5-pico). Each button taps onResponse with the number string.
         - If poll.pollType == "block_quality": 4 buttons in HStack (perdi, meh, ok, flow). Each taps onResponse with the value.
         - Each button: Text label, DS.header background, cornerRadius 8, padding. On tap: visual feedback (scale animation), call onResponse.
         - "Lembrar em 10min" button below (secondary style, DS.muted color)
       - Background DS.bg, padding 20
       - Design: quick interaction feel -- big tap targets, clear labels

    2. Create iOS_GateSheetView.swift (new file, #if os(iOS)):
       - Takes gate: GateSSEEvent, onDismiss: () -> Void
       - VStack layout:
         - Severity indicator: colored circle (DS.amber or DS.red based on gate.severity) + "Alerta" label
         - Gate message text (DS.font(16, .medium))
         - If gate.tasks is non-empty: task list with bullet points
         - "Dispensar" button (DS.muted background, calls onDismiss)
       - Background DS.bg, padding 20

    3. Update iOS_TabRootView.swift:
       - Replace placeholder poll sheet with iOS_PollSheetView(poll:onResponse:)
       - Replace placeholder gate sheet with iOS_GateSheetView(gate:onDismiss:)
       - onResponse closure: POST to /v1/polls/{poll.pollId}/respond with {"value": value} using ErestorConfig, then set activePoll = nil
       - Add respondToPoll helper function (async, URLSession POST)

    4. Update ErestorApp_iOS.swift AppDelegate_iOS:
       - In didFinishLaunchingWithOptions, register notification categories:
         - POLL_ENERGY: 4 actions (per iOS 4-button limit): ENERGY_12 "1-2 baixa", ENERGY_3 "3 ok", ENERGY_45 "4-5 alta", REMIND_10 "Lembrar 10min". NOTE: iOS limits to 4 action buttons. Full 5-option UI is in the modal sheet when notification is tapped.
         - POLL_QUALITY: 4 actions: QUALITY_perdi "perdi", QUALITY_meh "meh", QUALITY_ok "ok", QUALITY_flow "flow"
         - GATE_INFORM: 2 actions: GATE_VER "Ver", GATE_DISPENSAR "Dispensar"
         - Set categories via UNUserNotificationCenter.current().setNotificationCategories()
       - Add userNotificationCenter(_:didReceive:withCompletionHandler:) method:
         - Parse action identifier (ENERGY_12 -> "2", ENERGY_3 -> "3", ENERGY_45 -> "4", QUALITY_* -> value, REMIND_10 -> schedule reminder)
         - Extract poll_id from notification userInfo
         - POST to /v1/polls/{poll_id}/respond with parsed value
         - For REMIND_10: schedule local notification in 10 minutes with same category
         - For GATE_VER: bring app to foreground (UIApplication.shared.open on main tab)
  </action>
  <verify>
    <automated>cd ~/projetos/erestor/ErestorApp && xcodegen generate && xcodebuild -project ErestorApp.xcodeproj -scheme ErestorApp-iOS -destination 'platform=iOS Simulator,name=iPhone 16' -quiet build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - iOS_PollSheetView renders energy (5 buttons) and quality (4 buttons) polls as modal sheets
    - iOS_GateSheetView renders gate alerts with severity colors and task list
    - Notification categories registered with action buttons (4-button limit respected)
    - Notification response handler POSTs to /v1/polls/{id}/respond
    - 10-min reminder notification scheduled on REMIND_10 action
    - iOS build succeeds
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Backend APNs integration in poll scheduler</name>
  <files>
    ~/claude-sync/produtividade/api/routers/events.py
    ~/claude-sync/produtividade/tests/test_apns_integration.py
  </files>
  <behavior>
    - Test: when poll scheduler creates an energy poll, _send_apns_for_poll is called with category POLL_ENERGY
    - Test: when poll scheduler creates a quality poll, _send_apns_for_poll is called with category POLL_QUALITY
    - Test: when poll scheduler creates a gate alert, _send_apns_for_gate is called with urgent sound
    - Test: APNs send failure does not crash the scheduler (graceful error handling)
    - Test: APNs is not called when apns.is_configured returns False
  </behavior>
  <action>
    1. Create tests/test_apns_integration.py:
       - Mock APNs class and devices.json loading
       - Test _send_apns_for_poll with energy poll type -> verifies category="POLL_ENERGY"
       - Test _send_apns_for_poll with block_quality type -> verifies category="POLL_QUALITY"
       - Test _send_apns_for_gate -> verifies sound="critical" or similar urgent indicator
       - Test APNs not configured -> returns gracefully (no exception)
       - Test APNs send raises exception -> logged, no crash

    2. Add APNs helper functions to events.py (at module level, below existing _poll_scheduler):
       - async def _send_apns_for_poll(poll_type: str, poll_id: str, question: str) -> None:
         - Lazy import: from erestor.apns import APNs
         - Load devices from data/devices.json (Path.home() / "claude-sync/produtividade/erestor/data/devices.json" on server, fall back gracefully)
         - If no iOS device or APNs not configured: return silently
         - category = "POLL_ENERGY" if poll_type == "energy" else "POLL_QUALITY"
         - Call await asyncio.to_thread(apns.send_push, device_token=..., title="Erestor", body=question, category=category, data={"poll_id": poll_id, "poll_type": poll_type})
         - Wrap in try/except, log warnings on failure
       - async def _send_apns_for_gate(gate_id: str, message: str) -> None:
         - Same pattern but category="GATE_INFORM", include sound="default" in APNs payload
         - data={"gate_id": gate_id}

    3. Wire APNs calls into _poll_scheduler in events.py:
       - After each push_event("poll", ...) call for energy polls: await _send_apns_for_poll("energy", poll_id, question)
       - After each push_event("poll", ...) call for quality polls: await _send_apns_for_poll("block_quality", poll_id, question)
       - After each push_event("gate", ...) call: await _send_apns_for_gate(gate_id, message)
       - Per discretion recommendation: always send to iOS regardless of macOS presence (no deduplication logic needed)
  </action>
  <verify>
    <automated>cd ~/claude-sync/produtividade && python3 -m pytest tests/test_apns_integration.py -x --timeout=30 -v</automated>
  </verify>
  <done>
    - Backend poll scheduler sends APNs push for every energy poll, quality poll, and gate alert
    - APNs failures are logged but never crash the scheduler
    - APNs skipped gracefully when not configured
    - Always sends to iOS (no presence-based deduplication per discretion decision)
    - All APNs integration tests pass
  </done>
</task>

</tasks>

<verification>
- iOS build: `cd ~/projetos/erestor/ErestorApp && xcodegen generate && xcodebuild -project ErestorApp.xcodeproj -scheme ErestorApp-iOS -destination 'platform=iOS Simulator,name=iPhone 16' build`
- Backend tests: `cd ~/claude-sync/produtividade && python3 -m pytest tests/test_apns_integration.py -x --timeout=30`
- macOS build unaffected: `xcodebuild -project ErestorApp.xcodeproj -scheme ErestorApp -quiet build 2>&1 | tail -3`
</verification>

<success_criteria>
- Polls appear as quick modal sheets on iOS (tap response, sheet dismisses)
- Gate alerts appear as modal sheets with severity and tasks
- Push notifications have inline action buttons for poll responses
- Notification actions POST responses to backend
- Backend sends APNs for every poll and gate event
- APNs gracefully degrades when not configured
</success_criteria>

<output>
After completion, create `.planning/phases/03-ios-data-migration/03-04-SUMMARY.md`
</output>
