---
phase: 03-ios-data-migration
plan: 03
type: execute
wave: 2
depends_on: [03-02]
files_modified:
  - ~/projetos/erestor/ErestorApp/ErestorApp/Views/iOS_AgendaView.swift
  - ~/projetos/erestor/ErestorApp/ErestorApp/Views/iOS_EventDetailSheet.swift
  - ~/projetos/erestor/ErestorApp/ErestorApp/Views/iOS_InsightsView.swift
  - ~/projetos/erestor/ErestorApp/ErestorApp/Views/iOS_TabRootView.swift
  - ~/projetos/erestor/ErestorApp/ErestorApp/Services/ChatService.swift
autonomous: true
requirements: [IOS-02]

must_haves:
  truths:
    - "Agenda tab shows a vertical timeline with hours on the side and event blocks positioned by time"
    - "Current event is expanded with progress bar and associated tasks"
    - "Swiping left/right navigates between days"
    - "Tapping an event opens a half-sheet with full details"
    - "Insights tab shows energy trend chart, quality distribution, and timer hours"
  artifacts:
    - path: "ErestorApp/ErestorApp/Views/iOS_AgendaView.swift"
      provides: "Day timeline with time-positioned event blocks, swipe navigation, current event expansion"
      contains: "TabView.*page"
    - path: "ErestorApp/ErestorApp/Views/iOS_EventDetailSheet.swift"
      provides: "Half-sheet with event details (description, tasks, notes)"
      contains: "presentationDetents"
    - path: "ErestorApp/ErestorApp/Views/iOS_InsightsView.swift"
      provides: "Insights tab with Swift Charts for energy, quality, timer data"
      contains: "import Charts"
  key_links:
    - from: "iOS_AgendaView.swift"
      to: "ChatService"
      via: "chatService.context?.events for day events"
      pattern: "chatService.*events"
    - from: "iOS_InsightsView.swift"
      to: "/v1/insights/chart-data"
      via: "URLSession fetch on appear"
      pattern: "insights/chart-data"
    - from: "iOS_TabRootView.swift"
      to: "iOS_AgendaView + iOS_InsightsView"
      via: "TabView tabs replacing placeholders"
      pattern: "iOS_AgendaView\\|iOS_InsightsView"
---

<objective>
Build the Agenda tab with a vertical day timeline (Google Calendar-style) with swipe navigation and event detail sheets, and the Insights tab with Swift Charts visualizations for energy trends, block quality distribution, and timer hours.

Purpose: IOS-02 requires a full day agenda view. The Insights tab makes historical data visually accessible per MIGR requirements and user's locked 4-tab decision.

Output: Functional Agenda and Insights tabs replacing the placeholders from Plan 03-02.
</objective>

<execution_context>
@/Users/imacke/.claude/get-shit-done/workflows/execute-plan.md
@/Users/imacke/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ios-data-migration/03-02-SUMMARY.md
@~/projetos/erestor/ErestorApp/ErestorApp/Views/iOS_TabRootView.swift
@~/projetos/erestor/ErestorApp/ErestorApp/Views/DayTimelineView.swift
@~/projetos/erestor/ErestorApp/ErestorApp/Models/Message.swift
@~/projetos/erestor/ErestorApp/ErestorApp/Services/ChatService.swift
@~/projetos/erestor/ErestorApp/ErestorApp/Services/ErestorConfig.swift
@~/projetos/erestor/ErestorApp/ErestorApp/Views/DesignSystem.swift

<interfaces>
From Message.swift:
```swift
struct GCalEvent: Codable, Identifiable {
    let id: String?
    let summary: String
    let start: String?
    let end: String?
    let description: String?
    let calendarName: String?
}

struct ContextSummary: Codable {
    let currentEvent: GCalEvent?
    let nextEvent: GCalEvent?
    let activeTimer: TimerInfo?
    let tasks: [TaskItem]?
    let events: [GCalEvent]?  // day events (if available from context)
}
```

From ErestorConfig.swift:
```swift
enum ErestorConfig {
    static func url(for path: String) -> URL?
    static func authorize(_ request: inout URLRequest)
}
```

From DayTimelineView.swift (macOS version, reference for iOS adaptation):
- Shows events in a vertical timeline layout
- iOS version should be a new file, not modify the macOS one
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Agenda tab with vertical timeline, swipe navigation, and event detail sheet</name>
  <files>
    ~/projetos/erestor/ErestorApp/ErestorApp/Views/iOS_AgendaView.swift
    ~/projetos/erestor/ErestorApp/ErestorApp/Views/iOS_EventDetailSheet.swift
    ~/projetos/erestor/ErestorApp/ErestorApp/Views/iOS_TabRootView.swift
  </files>
  <action>
    1. Create iOS_AgendaView.swift (new file, #if os(iOS)):
       - Takes @ObservedObject chatService: ChatService
       - @State selectedDate: Date = Date() displayed at top as formatted date ("Ter, 10 Mar 2026")
       - Swipe navigation: TabView with .tabViewStyle(.page(indexDisplayMode: .never)) containing 3 pages (yesterday, today, tomorrow). On page change, update selectedDate and fetch events for that day.
       - Each page is a ScrollView with a ZStack timeline:
         - Hour markers on the left: ForEach 6..<23 showing "HH:00" text at proportional Y offsets
         - hourHeight = 60 points per hour
         - Event blocks positioned by start time offset: CGFloat(startHour - 6) * hourHeight
         - Event block height: duration in hours * hourHeight (minimum 30pt)
         - Current event: expanded card with progress bar (use EventCardView or custom expanded view showing summary, time, progress, and tasks)
         - Other events: compact card (summary + time range, DS.header background, rounded corners)
         - Current time indicator: red line at current hour position (visible only on today)
       - Tapping any event sets @State selectedEvent, which presents iOS_EventDetailSheet
       - Fetch events: on appear and on date change, call GET /v1/calendar/today?date=YYYY-MM-DD (or use chatService.context?.events for today). If the calendar endpoint does not support a date parameter, use today's events from context for today and show "Sem eventos" for other days.
       - Background DS.bg, auto-scroll to current hour on appear (ScrollViewReader)

    2. Create iOS_EventDetailSheet.swift (new file, #if os(iOS)):
       - Takes event: GCalEvent
       - Presented as .sheet with .presentationDetents([.medium])
       - Content: VStack with event summary (DS.font(18, .semibold)), time range, calendar name badge, description text (if present), associated tasks (if present)
       - Background DS.header, padding 20
       - Drag indicator visible

    3. Update iOS_TabRootView.swift:
       - Replace Agenda placeholder Text("Agenda") with iOS_AgendaView(chatService: chatService)
  </action>
  <verify>
    <automated>cd ~/projetos/erestor/ErestorApp && xcodegen generate && xcodebuild -project ErestorApp.xcodeproj -scheme ErestorApp-iOS -destination 'platform=iOS Simulator,name=iPhone 16' -quiet build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Agenda tab shows vertical timeline with hour markers and event blocks
    - Current event is expanded with progress indicator
    - Swiping navigates between days
    - Tapping event opens half-sheet with details
    - Current time red line visible on today's view
  </done>
</task>

<task type="auto">
  <name>Task 2: Insights tab with Swift Charts</name>
  <files>
    ~/projetos/erestor/ErestorApp/ErestorApp/Views/iOS_InsightsView.swift
    ~/projetos/erestor/ErestorApp/ErestorApp/Views/iOS_TabRootView.swift
  </files>
  <action>
    1. Create iOS_InsightsView.swift (new file, #if os(iOS)):
       - Takes @ObservedObject chatService: ChatService
       - @State chartData: InsightsChartData? (local struct with energy_trend, quality_distribution, timer_hours)
       - @State selectedPeriod: String = "14d" (picker: 7d, 14d, 30d)
       - On appear and on period change: fetch GET /v1/insights/chart-data?period={selectedPeriod} using ErestorConfig.url + authorize
       - Layout: ScrollView > VStack(spacing: 20):
         - Period picker: Picker with .segmented style (7 dias, 14 dias, 30 dias)
         - Summary cards row: HStack with 3 compact cards showing today's energy level, blocks completed, hours worked (from latest data points). DS.header background, rounded.
         - Energy trend chart: import Charts, use LineMark + PointMark for energy_trend data. Y axis 1-5, X axis dates. .foregroundStyle(DS.green). Title "Energia" above chart.
         - Quality distribution chart: BarMark for quality_distribution. 4 bars (perdi, meh, ok, flow) with colors (DS.red, DS.amber, DS.muted, DS.green). Title "Qualidade dos blocos".
         - Timer hours chart: BarMark for timer_hours by date. .foregroundStyle(DS.green). Title "Horas trabalhadas".
       - Each chart section: card background DS.header, cornerRadius 12, padding 16
       - Loading state: ProgressView while chartData is nil
       - Error state: "Sem dados disponíveis" if fetch fails
       - Background DS.bg

    2. Define InsightsChartData locally in the file:
       ```swift
       struct InsightsChartData: Codable {
           let energyTrend: [EnergyPoint]
           let qualityDistribution: QualityDist
           let timerHours: [TimerPoint]
       }
       struct EnergyPoint: Codable, Identifiable { var id: String { date }; let date: String; let level: Int }
       struct QualityDist: Codable { let perdi: Int; let meh: Int; let ok: Int; let flow: Int }
       struct TimerPoint: Codable, Identifiable { var id: String { date }; let date: String; let hours: Double }
       ```

    3. Update iOS_TabRootView.swift:
       - Replace Insights placeholder Text("Insights") with iOS_InsightsView(chatService: chatService)
  </action>
  <verify>
    <automated>cd ~/projetos/erestor/ErestorApp && xcodegen generate && xcodebuild -project ErestorApp.xcodeproj -scheme ErestorApp-iOS -destination 'platform=iOS Simulator,name=iPhone 16' -quiet build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Insights tab shows energy trend line chart, quality distribution bar chart, timer hours bar chart
    - Period picker switches between 7d, 14d, 30d views
    - Summary cards show today's key metrics
    - Charts use DS colors (green, amber, red, muted)
    - Loading and empty states handled gracefully
  </done>
</task>

</tasks>

<verification>
- iOS build succeeds: `cd ~/projetos/erestor/ErestorApp && xcodegen generate && xcodebuild -project ErestorApp.xcodeproj -scheme ErestorApp-iOS -destination 'platform=iOS Simulator,name=iPhone 16' build`
- macOS build unaffected: `xcodebuild -project ErestorApp.xcodeproj -scheme ErestorApp -quiet build 2>&1 | tail -3`
- All 4 tabs functional (Painel, Chat, Agenda, Insights)
</verification>

<success_criteria>
- Agenda tab displays vertical timeline with positioned event blocks
- Current event expanded, others compact
- Swipe navigates days, tap opens detail sheet
- Insights tab shows charts with real data from /v1/insights/chart-data
- Both tabs use DS design system consistently
</success_criteria>

<output>
After completion, create `.planning/phases/03-ios-data-migration/03-03-SUMMARY.md`
</output>
