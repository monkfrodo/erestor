---
phase: 03-ios-data-migration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/projetos/erestor/ErestorApp/ErestorApp/iOS/ErestorApp_iOS.swift
  - ~/projetos/erestor/ErestorApp/ErestorApp/Views/iOS_TabRootView.swift
  - ~/projetos/erestor/ErestorApp/ErestorApp/Views/iOS_PainelView.swift
autonomous: true
requirements: [IOS-01]

must_haves:
  truths:
    - "iOS app launches with a 4-tab TabView (Painel, Chat, Agenda, Insights)"
    - "Painel tab shows stacked cards for current event, timer, next event, and tasks"
    - "Chat tab displays the existing ChatHistoryView + ChatInputView"
    - "SSE stream reconnects when app returns to foreground"
    - "Polls and gates arriving via SSE trigger modal bottom sheets"
  artifacts:
    - path: "ErestorApp/ErestorApp/iOS/ErestorApp_iOS.swift"
      provides: "iOS entry point with TabView root, notification categories, scenePhase lifecycle"
      contains: "iOS_TabRootView"
    - path: "ErestorApp/ErestorApp/Views/iOS_TabRootView.swift"
      provides: "TabView wrapper with 4 tabs, poll/gate sheet bindings, SSE lifecycle"
      contains: "TabView"
    - path: "ErestorApp/ErestorApp/Views/iOS_PainelView.swift"
      provides: "Painel tab with stacked cards reusing shared views"
      contains: "EventCardView"
  key_links:
    - from: "iOS_TabRootView.swift"
      to: "ChatService"
      via: "@ObservedObject chatService shared across all tabs"
      pattern: "ObservedObject.*chatService"
    - from: "iOS_TabRootView.swift"
      to: "iOS_PainelView"
      via: "TabView tab item"
      pattern: "tabItem.*Painel"
    - from: "ErestorApp_iOS.swift"
      to: "iOS_TabRootView"
      via: "WindowGroup root view"
      pattern: "iOS_TabRootView"
---

<objective>
Create the iOS app foundation: transform the single-view app into a tab-based experience with Painel (stacked context cards), Chat, and placeholder tabs for Agenda and Insights. Wire SSE lifecycle to scenePhase for reliable foreground/background transitions.

Purpose: IOS-01 requires a contextual panel adapted for iPhone. The tab structure is the locked decision from user context. This plan establishes the iOS app shell that all subsequent plans build into.

Output: Functional iOS app with TabView, Painel tab showing real-time context, Chat tab with existing shared views, and SSE reconnection on foreground.
</objective>

<execution_context>
@/Users/imacke/.claude/get-shit-done/workflows/execute-plan.md
@/Users/imacke/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@~/projetos/erestor/ErestorApp/ErestorApp/iOS/ErestorApp_iOS.swift
@~/projetos/erestor/ErestorApp/project.yml
@~/projetos/erestor/ErestorApp/ErestorApp/Services/ChatService.swift
@~/projetos/erestor/ErestorApp/ErestorApp/Views/ContextPanelView.swift
@~/projetos/erestor/ErestorApp/ErestorApp/Views/EventCardView.swift
@~/projetos/erestor/ErestorApp/ErestorApp/Views/TimerChipView.swift
@~/projetos/erestor/ErestorApp/ErestorApp/Views/NextEventView.swift
@~/projetos/erestor/ErestorApp/ErestorApp/Views/TaskListView.swift
@~/projetos/erestor/ErestorApp/ErestorApp/Views/ChatHistoryView.swift
@~/projetos/erestor/ErestorApp/ErestorApp/Views/ChatInputView.swift
@~/projetos/erestor/ErestorApp/ErestorApp/Views/DesignSystem.swift
@~/projetos/erestor/ErestorApp/ErestorApp/Models/SSEEvent.swift

<interfaces>
<!-- Key types the executor needs -->
From ChatService.swift:
```swift
@MainActor
class ChatService: ObservableObject {
    @Published var context: ContextSummary?
    @Published var messages: [ChatMessage] = []
    @Published var isStreaming: Bool = false
    @Published var serverOnline: Bool = true
    @Published var activePolls: [PollSSEEvent] = []
    @Published var activeGates: [GateSSEEvent] = []
    func startEventStream()
    func stopEventStream()
    func sendMessage(_ text: String) async
}
```

From SSEEvent.swift:
```swift
struct PollSSEEvent: Identifiable, Codable { var id: String { pollId }; let pollId: String; let pollType: String; let question: String; let options: [String]; let expiresAt: String? }
struct GateSSEEvent: Identifiable, Codable { var id: String { gateId }; let gateId: String; let message: String; let severity: String; let tasks: [String]? }
```

From DesignSystem.swift:
```swift
enum DS {
    static let bg, header, text, muted, border, green, amber, red: Color
    static func font(_ size: CGFloat, weight: Font.Weight) -> Font
}
```

From ContextPanelView.swift (macOS layout -- iOS should NOT use this directly, build iOS_PainelView instead):
- Shows EventCardView, TimerChipView, NextEventView, CollapsibleTasksView, polls/gates, chat
- iOS needs stacked cards layout without the chat section (chat is its own tab)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: iOS TabView root + Painel tab with stacked context cards</name>
  <files>
    ~/projetos/erestor/ErestorApp/ErestorApp/Views/iOS_TabRootView.swift
    ~/projetos/erestor/ErestorApp/ErestorApp/Views/iOS_PainelView.swift
    ~/projetos/erestor/ErestorApp/ErestorApp/iOS/ErestorApp_iOS.swift
  </files>
  <action>
    1. Create iOS_TabRootView.swift (new file, iOS-only via #if os(iOS)):
       - Takes @ObservedObject chatService: ChatService
       - TabView with selection binding and 4 tabs:
         - Tab 0 "Painel": iOS_PainelView(chatService:) with systemImage "square.grid.2x2"
         - Tab 1 "Chat": VStack of ChatHistoryView + ChatInputView (reuse shared views) with systemImage "bubble.left"
         - Tab 2 "Agenda": placeholder Text("Agenda") with systemImage "calendar" (Plan 03-03 fills this)
         - Tab 3 "Insights": placeholder Text("Insights") with systemImage "chart.line.uptrend.xyaxis" (Plan 03-04 fills this)
       - .tint(DS.green) on TabView
       - .preferredColorScheme(.dark)
       - Poll sheet binding: .sheet(item: $activePoll) presenting iOS_PollSheetView (placeholder for now -- use simple VStack with poll question and option buttons that POST response)
       - Gate sheet binding: .sheet(item: $activeGate) presenting iOS_GateSheetView (placeholder -- simple VStack with gate message and dismiss)
       - .presentationDetents([.fraction(0.35)]) on poll sheet, [.medium] on gate sheet
       - .onReceive(chatService.$activePolls) to set activePoll when new poll arrives
       - .onReceive(chatService.$activeGates) to set activeGate when new gate arrives
       - Environment scenePhase observer: on .active call chatService.startEventStream(), on .background call chatService.stopEventStream()

    2. Create iOS_PainelView.swift (new file, iOS-only via #if os(iOS)):
       - Takes @ObservedObject chatService: ChatService
       - ScrollView with VStack(spacing: 16) of stacked cards:
         - Card 1: EventCardView (current event from chatService.context) wrapped in card styling (DS.header background, rounded corners 12, padding)
         - Card 2: TimerChipView (if timer active) wrapped in same card styling
         - Card 3: NextEventView (next event from chatService.context) wrapped in card styling
         - Card 4: TaskListView (tasks from chatService.context) wrapped in card styling
       - Each card: background DS.header, cornerRadius 12, padding 16
       - Show "Sem dados" placeholder if chatService.context is nil
       - Pull-to-refresh that calls chatService.startEventStream() to force reconnect
       - .padding(.horizontal, 16) on outer VStack
       - Background DS.bg

    3. Update ErestorApp_iOS.swift:
       - Replace ContextPanelView(chatService:) with iOS_TabRootView(chatService:)
       - Keep existing AppDelegate_iOS with APNs setup unchanged
       - Remove .ignoresSafeArea(edges: .bottom) (TabView handles safe area)
  </action>
  <verify>
    <automated>cd ~/projetos/erestor/ErestorApp && xcodegen generate && xcodebuild -project ErestorApp.xcodeproj -scheme ErestorApp-iOS -destination 'platform=iOS Simulator,name=iPhone 16' -quiet build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - iOS app builds successfully for iOS Simulator
    - App launches with 4-tab TabView (Painel, Chat, Agenda, Insights)
    - Painel tab shows stacked cards for event, timer, next event, tasks
    - Chat tab shows existing ChatHistoryView + ChatInputView
    - Agenda and Insights tabs show placeholders
    - SSE reconnects on foreground via scenePhase
    - Poll/gate sheets have basic placeholder implementations
  </done>
</task>

</tasks>

<verification>
- `cd ~/projetos/erestor/ErestorApp && xcodegen generate && xcodebuild -project ErestorApp.xcodeproj -scheme ErestorApp-iOS -destination 'platform=iOS Simulator,name=iPhone 16' build` -- builds without errors
- macOS target still builds: `xcodebuild -project ErestorApp.xcodeproj -scheme ErestorApp -quiet build 2>&1 | tail -3`
</verification>

<success_criteria>
- iOS app has 4-tab structure per user's locked decision
- Painel shows real-time context from SSE in stacked card layout
- Chat tab reuses shared views without duplication
- SSE lifecycle tied to scenePhase (reconnect on foreground, disconnect on background)
- macOS build unaffected
</success_criteria>

<output>
After completion, create `.planning/phases/03-ios-data-migration/03-02-SUMMARY.md`
</output>
