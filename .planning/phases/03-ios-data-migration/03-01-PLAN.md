---
phase: 03-ios-data-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/claude-sync/produtividade/migrate-history.py
  - ~/claude-sync/produtividade/tests/test_migration.py
  - ~/claude-sync/produtividade/api/routers/insights.py
  - ~/claude-sync/produtividade/api/app.py
  - ~/claude-sync/produtividade/tests/test_insights.py
autonomous: true
requirements: [MIGR-01, MIGR-02, MIGR-03]

must_haves:
  truths:
    - "Historical mood/energy data from weekly-signals.json is in SQLite daily_signals table"
    - "Memory system data (people, context, behavior-model) is in SQLite memory_people and memory_context tables"
    - "Log history events are in SQLite event_log table"
    - "Migration script can re-run safely without creating duplicates"
    - "Chart data endpoint returns aggregated energy/quality data for a date range"
  artifacts:
    - path: "~/claude-sync/produtividade/migrate-history.py"
      provides: "Extended migration with memory system tables and idempotent inserts"
      contains: "memory_people"
    - path: "~/claude-sync/produtividade/api/routers/insights.py"
      provides: "GET /v1/insights/chart-data endpoint for aggregated signals"
      exports: ["router"]
    - path: "~/claude-sync/produtividade/tests/test_migration.py"
      provides: "Migration tests for signals, memory, and logs"
      contains: "test_signals"
    - path: "~/claude-sync/produtividade/tests/test_insights.py"
      provides: "Insights endpoint tests"
      contains: "test_chart_data"
  key_links:
    - from: "migrate-history.py"
      to: "erestor_events.db"
      via: "SQLite INSERT OR IGNORE"
      pattern: "INSERT OR IGNORE INTO"
    - from: "api/routers/insights.py"
      to: "erestor_events.db"
      via: "SQLite SELECT with date range"
      pattern: "daily_signals.*WHERE.*date"
---

<objective>
Extend the data migration script to cover all historical Telegram-era data (memory system: people, context, behavior-model) and add a backend API endpoint for chart data that the iOS Insights tab will consume.

Purpose: MIGR-01/02/03 require all historical data to be accessible in the new system. The migration script already handles signals and logs but needs memory system coverage. The Insights API provides pre-aggregated data so chart rendering does not require a Claude API call.

Output: Extended migrate-history.py with memory tables, insights API router, and tests for both.
</objective>

<execution_context>
@/Users/imacke/.claude/get-shit-done/workflows/execute-plan.md
@/Users/imacke/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@~/claude-sync/produtividade/migrate-history.py
@~/claude-sync/produtividade/erestor_events.db
@~/claude-sync/produtividade/memory/people/
@~/claude-sync/produtividade/memory/context/
@~/claude-sync/produtividade/api/app.py
@~/claude-sync/produtividade/api/routers/polls.py

<interfaces>
<!-- Existing migration script patterns -->
From migrate-history.py:
- init_db(conn) creates event_log and daily_signals tables
- migrate_signals(conn) imports weekly-signals.json with INSERT OR IGNORE
- migrate_logs(conn) extracts timer/task events from logs/*.md
- All functions return count of migrated records

From api/app.py:
- FastAPI app with router includes: app.include_router(router, prefix="/v1")
- Auth dependency: Depends(verify_token) at router level

From api/routers/polls.py (pattern reference):
- Router with router = APIRouter(prefix="/polls", tags=["polls"], dependencies=[Depends(verify_token)])
- Uses event_store helpers for SQLite access
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend migration script with memory system tables and tests</name>
  <files>
    ~/claude-sync/produtividade/migrate-history.py
    ~/claude-sync/produtividade/tests/test_migration.py
  </files>
  <behavior>
    - Test: migrate_memory_people inserts all people/*.md files into memory_people table with name, role, status, raw_markdown
    - Test: migrate_memory_context inserts context/*.md and context/*.json files into memory_context table with filename, content_type, data_json, raw_text
    - Test: behavior-model.json imported as content_type='behavior_model' with JSON in data_json column
    - Test: goals.json imported as content_type='goals' with JSON in data_json column
    - Test: weekly-signals.json.bak imported as content_type='signals' (existing signals migration covers daily_signals, this covers the raw JSON backup)
    - Test: running migration twice produces no duplicates (INSERT OR IGNORE + UNIQUE constraints)
    - Test: migrate_signals still works (regression)
    - Test: migrate_logs still works (regression)
  </behavior>
  <action>
    1. Create tests/test_migration.py with in-memory SQLite:
       - test_signals: create temp weekly-signals.json, run migrate_signals, verify rows in daily_signals
       - test_memory_people: create temp people/*.md files, run migrate_memory_people, verify rows in memory_people (name extracted from filename kebab-to-title, raw_markdown stored)
       - test_memory_context: create temp context files (behavior-model.json, goals.json, patterns.md), run migrate_memory_context, verify content_type and data_json/raw_text
       - test_logs: create temp log file with timer and task patterns, run migrate_logs, verify event_log rows
       - test_idempotent: run full migration twice on same data, verify no duplicate rows

    2. Extend migrate-history.py:
       - Add init_memory_tables(conn) creating memory_people (id, name UNIQUE, role, status, raw_markdown, created_at) and memory_context (id, filename UNIQUE, content_type, data_json, raw_text, created_at)
       - Add migrate_memory_people(conn) that reads memory/people/*.md, extracts name from H1 heading or filename, parses role/status from "**Papel:**" and "**Status:**" lines, stores full markdown as raw_markdown. Uses INSERT OR IGNORE with name UNIQUE.
       - Add migrate_memory_context(conn) that reads memory/context/*.json (load as JSON, store in data_json) and memory/context/*.md (store raw text in raw_text, empty data_json). content_type derived from filename (behavior-model.json -> 'behavior_model', goals.json -> 'goals', patterns.md -> 'patterns', etc.). Uses INSERT OR IGNORE with filename UNIQUE.
       - Update main() to call init_memory_tables and both new migration functions
       - Keep existing migrate_signals, migrate_logs, migrate_phone unchanged (backward compatible)
  </action>
  <verify>
    <automated>cd ~/claude-sync/produtividade && python3 -m pytest tests/test_migration.py -x --timeout=30 -v</automated>
  </verify>
  <done>
    - memory_people and memory_context tables created with proper schema
    - All people/*.md files imported with name, role, status, raw_markdown
    - All context/*.json files imported with parsed JSON in data_json
    - All context/*.md files imported with raw text in raw_text
    - Migration is idempotent (re-run produces no duplicates)
    - Existing signal and log migrations unaffected
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Insights chart-data API endpoint</name>
  <files>
    ~/claude-sync/produtividade/api/routers/insights.py
    ~/claude-sync/produtividade/api/app.py
    ~/claude-sync/produtividade/tests/test_insights.py
  </files>
  <behavior>
    - Test: GET /v1/insights/chart-data?period=7d returns energy levels aggregated by day for last 7 days
    - Test: GET /v1/insights/chart-data?period=30d returns 30 days of data
    - Test: Response includes energy_trend (date+level pairs), quality_distribution (perdi/meh/ok/flow counts), timer_hours (date+hours pairs)
    - Test: Empty database returns empty arrays (not error)
    - Test: Endpoint requires auth token
  </behavior>
  <action>
    1. Create tests/test_insights.py following existing test patterns (async test client, auth_headers fixture):
       - Seed test SQLite with sample daily_signals and poll_responses data
       - Test chart-data endpoint returns correct aggregations
       - Test period parameter filtering
       - Test empty database graceful handling

    2. Create api/routers/insights.py:
       - Router with prefix="/insights", tags=["insights"], dependencies=[Depends(verify_token)]
       - GET /chart-data endpoint accepting query param period (7d, 14d, 30d, default 14d)
       - Query daily_signals for energy values in date range, return as energy_trend: [{date, level}]
       - Query poll_responses for block_quality polls in date range, count by value, return as quality_distribution: {perdi: N, meh: N, ok: N, flow: N}
       - Query daily_signals for timer hours in date range, return as timer_hours: [{date, hours}]
       - Return ApiResponse envelope with {energy_trend, quality_distribution, timer_hours}

    3. Update api/app.py to include insights router:
       - Import and include: app.include_router(insights_router, prefix="/v1")
  </action>
  <verify>
    <automated>cd ~/claude-sync/produtividade && python3 -m pytest tests/test_insights.py -x --timeout=30 -v</automated>
  </verify>
  <done>
    - GET /v1/insights/chart-data returns aggregated energy, quality, and timer data
    - Period parameter filters by 7d/14d/30d
    - Response follows ApiResponse envelope pattern
    - Empty database returns empty arrays gracefully
    - Auth required (401 without token)
  </done>
</task>

</tasks>

<verification>
- `cd ~/claude-sync/produtividade && python3 -m pytest tests/test_migration.py tests/test_insights.py -x --timeout=30` -- all tests pass
- `python3 ~/claude-sync/produtividade/migrate-history.py` -- runs without error, reports migrated record counts including memory system
- `python3 ~/claude-sync/produtividade/migrate-history.py` -- running again reports 0 new records (idempotent)
</verification>

<success_criteria>
- All historical data (signals, logs, people, context) migrated to SQLite
- Migration is idempotent and safe to re-run
- Insights API endpoint serves pre-aggregated chart data
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-ios-data-migration/03-01-SUMMARY.md`
</output>
