---
phase: 02-macos-experience
plan: 06
type: execute
wave: 4
depends_on: [02-03, 02-04, 02-05]
files_modified: []
autonomous: false
requirements: [PANEL-01, PANEL-02, PANEL-03, PANEL-04, PANEL-05, PANEL-06, PANEL-07, CHAT-01, CHAT-02, CHAT-03, CHAT-04, DATA-01, DATA-02, DATA-03, DATA-04, NOTF-01, SYNT-01, SYNT-02]

must_haves:
  truths:
    - "Kevin can see current event, timer, next event, and tasks in the panel"
    - "Kevin can chat with Erestor and see streaming markdown responses"
    - "Polls appear inline and as notifications"
    - "Gate alerts fire when blocks end with open tasks"
    - "Synthesis data is accessible via chat"
    - "Panel updates in real-time without polling"
  artifacts: []
  key_links: []
---

<objective>
Visual and functional verification of the complete macOS experience.

Purpose: All code is written -- this checkpoint verifies that the full experience works as designed. Kevin tests the actual app: panel layout, chat streaming, poll interactions, gate alerts, notifications, and synthesis queries. This is the phase gate before marking Phase 2 complete.

Output: User approval or list of issues to fix.
</objective>

<execution_context>
@/Users/imacke/.claude/get-shit-done/workflows/execute-plan.md
@/Users/imacke/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-macos-experience/02-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Full macOS experience verification</name>
  <files></files>
  <action>
    Build the app and present verification steps to the user.

    What was built:
    - Native SwiftUI chat replacing WKWebView (markdown rendering, token-by-token streaming)
    - Real-time SSE event stream replacing all polling
    - Panel layout: Context > Tasks (collapsible) > Alerts > Chat
    - Energy and block quality polls (inline + notifications)
    - Gate alerts for blocks ending with open tasks
    - Notification actions that post poll responses to backend
    - Synthesis accessible via chat ("como foi meu dia?")

    How to verify:

    Step 1 -- Build and launch:
    ```bash
    cd ~/projetos/erestor/ErestorApp && xcodegen generate && xcodebuild -scheme ErestorApp -configuration Debug build
    ```
    Launch the built app.

    Step 2 -- Panel layout:
    - Toggle panel with Cmd+Shift+E
    - Verify hierarchy: current event at top, timer below, next event, tasks (collapsed), chat at bottom
    - Click tasks section to expand/collapse
    - Verify panel does not steal focus from other apps
    - Resize panel and verify it persists size

    Step 3 -- Chat streaming:
    - Type a message and send
    - Verify streaming: words appear one-by-one (not all at once)
    - Verify after streaming: message renders with markdown (bold, code blocks if applicable)
    - Verify conversation history: previous messages still visible
    - Try: "cria um evento de trabalho amanha as 9h" -- verify action parsing works

    Step 4 -- Real-time context updates:
    - Verify current event updates when calendar changes (no manual refresh needed)
    - Verify timer updates in real-time

    Step 5 -- Polls (use trigger endpoint):
    ```bash
    curl -X POST http://localhost:8767/v1/polls/trigger \
      -H "Authorization: Bearer $ERESTOR_API_TOKEN" \
      -H "Content-Type: application/json" \
      -d '{"poll_type": "energy"}'
    ```
    - Verify poll appears inline in panel
    - Close panel, trigger another poll -- verify macOS notification appears
    - Tap a response -- verify notification dismisses and response is stored

    Step 6 -- Synthesis:
    - In chat, type: "como foi meu dia?"
    - Verify response includes poll data and timer/block information

    Step 7 -- Backend tests pass:
    ```bash
    cd ~/claude-sync/produtividade && python -m pytest tests/ -x --timeout=10
    ```

    Resume signal: Type "approved" or describe issues to fix.
  </action>
  <verify>User visual and functional confirmation of all Phase 2 features</verify>
  <done>Kevin approves the panel layout, chat, polls, gates, notifications, and synthesis -- or issues identified for gap closure</done>
</task>

</tasks>

<verification>
User approval of the complete macOS experience.
</verification>

<success_criteria>
- Kevin approves the panel layout, chat, polls, gates, notifications, and synthesis
- Or: issues identified for gap closure
</success_criteria>

<output>
After completion, create `.planning/phases/02-macos-experience/02-06-SUMMARY.md`
</output>
